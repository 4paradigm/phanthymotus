# deploy/core 运行时环境变量
# 复制此文件为 .env，填入真实值（.env 已被 .gitignore 排除）

# Resource Center 地址（提供已审核的镜像列表）
# 默认值已指向生产环境，通常无需修改
# RESOURCE_CENTER_URL=https://motus.phanthy.com

# Agent Core 运行时配置
DB_PATH=/work/resource/data.db
