fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0
PyYAML>=6.0

# No pytest here on purpose. The suites run inside the images built from the
# PR (see tester.py), not in this container: perception's tests `skipif` their
# way past missing pythainlp / khanaa / jieba / pypinyin / wetext / kaldifst,
# so a hand-maintained mirror of its dependencies would not fail when it fell
# behind — it would quietly run ~10 fewer tests and still report green.
