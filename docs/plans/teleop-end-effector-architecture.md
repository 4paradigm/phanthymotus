# PICO 遥操：通用输入、Driver 解算与执行卡片

状态：2026-09-22 用户批准实施；本地开发、Driver 原生构建和机上隔离验证已通过。用户明确授权停止空闲项目及保留现场修改的 Core 增量后，已切换 Core、普通 ActuCore 与 Driver，禁执行配置下真实 ROS 预览回传通过。Canvas 注册地址兼容修复已部署复核，原 16 张接待卡保留，新增三张遥操卡及两条正向控制线、一条自动反馈线。2026-09-23 经用户批准，实体北京 PICO 已迁移到 release 0.3.18，完成邀请配对、冷启动自动重连及静态模型显示；真实 Canvas 三卡 Shadow 启停通过，已恢复原图且项目停止、Driver 无执行权。PICO 浏览器安装、手柄动态模型和新架构真机动作验收尚未完成。

## 目标与边界

完整交付 Canvas 配置 → PICO 安装配对 → 双臂遥操与显示 → 结束收臂。
普通 ActuCore 内保留一张 `teleop`；天轶 Driver 内新增 `motion_control`，为已有 `arm` 增加连续目标输入。
仅迁移天轶双臂，保留 G1、既有 VLA 和 motus.control/1；本轮不实现手腿控制，不增加独立服务。
机器人承担接待期间不操作设备；用户确认空闲后，先核对实际占用、运行版本、ROS 状态和构建条件，再按授权准备目标机验证。机上排查不自动开启执行权或运动。Core 仅改所需配置、绑定和下载入口，保护用户其他修改。

## 三层与反馈

- `teleop`：OpenXR/WSS/RTC、配对、输入有效性、双握把、相对映射、通用末端目标。
- `motion_control`：控制用 URDF/TCP/标定、FK/IK、碰撞、执行器映射、会话与收臂。
- `arm`：最新关节目标流、共享执行门、最终限位限速、厂商发布、实测反馈与停止。
- `arm → motion_control` 使用 Driver 内部实测快照和执行记录，不新增中转 topic 或 Canvas 边。
- `motion_control → teleop` 复用现有反馈 topic；Canvas 随正向连线自动建立并显示反馈边，不纳入启动依赖图。
- `teleop → PICO` 复用现有 WSS visualization；异步显示不阻塞控制。

## 协议与运行契约

- 新增 motus.control/2，保留 /1：沿用 mode/values 和 descriptor 中的 groups，补 boot/session/seq/source_seq/mapping_epoch、单调时钟期限和鉴权；groups 不重复放入命令。
- 上游 eef_pose 是描述坐标系下 xyz 米与 xyzw 单位四元数；下游 joint_position 是描述关节顺序的 rad。
- 同机 domain 42、版本化 JSON、最新目标；低频管理继续 MCP；秘密不发布到 topic。
- Driver info 声明端口、末端、坐标系、模型/标定版本、单位、资源；不能仅凭维数推断语义。
- 双臂使用一个原子命令，descriptor 声明左右各 7 关节的 arm_l/arm_r group，共用同一执行租约；后续手开合是独立 normalized 组，不混入 7 维位姿。
- 每次重新使能用同一份新鲜 FK 快照建立映射，丢弃旧映射代次及过期求解。求解不能延长原输入寿命。
- 执行循环 50 Hz，默认 1 rad/s；沿用已验证时效，不新增首次接管低速、累计行程或误差百分比门槛。
- 只有一个共享执行门：原动作和流输入均检查资源，避免重复速度/恢复状态机。
- 碰撞/不可达保持最后有效位置附近，不增加越界搜索；继续处理最新输入，有效后同会话恢复。
- 松握保持，重握重新建基准；结束停止接收输入，按实测反馈收臂至厂商模型双臂零位（本轮自然下垂目标，14 关节 q=0），确认后释放；立即停止不收臂。
- 真故障与短暂求解失败分开，停止未确认不能报告成功。禁止旧目标重放或重启后自动运动。
- Shadow 共用算法但隔离会话与缓存，无执行租约和硬件命令；Live 建立新会话与基准。
- Driver 生成实际/IK/最后有效参考线与失败原因，保持原平视方向、躯干和 held 显示，不用旧线伪装新解。

## 安装、配对与 Canvas

- Canvas 提供二维码、短地址、APK 版本。PICO 浏览器下载并经系统安装后，回页面点击“打开并连接这台机器人”。
- 深链接预填机器人身份、endpoint、证书与邀请；从安装器直接打开而没有邀请时进入发现页，不猜测机器人。
- 邀请由认证 Canvas 签发，15 分钟、单次使用、可撤销、绑定机器人；下载不消耗旧 120 秒配对窗口。
- APK 下载链接与单次配对邀请分开：下载票据在现有共享 SQLite 保存摘要和绝对期限，Core 重启不续期；重生成或鉴权撤销使旧链接失效，每链接最多三次含重试，串行且至少相隔两秒。票据不保存配对邀请和管理凭据。
- 已配对冷启动自动连接，提供取消/切换/撤销；身份变化停止重试；连接不授予运动权限。
- 固定签名 release APK 作为带版本/签名/SHA256 的制品进入普通 ActuCore 镜像，Core 受控代理下载。构建源为确定性 gzip，分别核对压缩制品与原 APK 的大小/哈希，用户下载原 APK。
- 不提交 APK 或私钥；不改 bot 服务、不新增遥操构建开关；签名不兼容时明确迁移，不静默卸载。
- 模板为 teleop → motion_control → arm，自动显示运动反馈边。按输出端口分别协商能力，保留旧单端口。
- 兼容 Driver 注册的 `http://localhost:<port>/mcp`：三段发现和绑定将确切 `localhost` 规范为 `127.0.0.1`，保留端口；数字回环继续支持，不通过 DNS 放开任意主机名。回归覆盖注册发现、三卡模板和双重执行绑定。
- 日常配对、模式、输入映射、模型/标定、速度、启停从卡片配置完成；Canvas/PICO 共用开始/结束收臂/立即停止状态。
- 下游准备完成后才允许使能；项目停止先收臂和释放再停止下游，失败保留状态。
- 缺模型不隐藏安装配对，单卡配置故障不能拖垮普通 ActuCore。

## 实施与验证

基线主仓 4edefe81e0abba7aed56f16368c8af4f561915d0；Driver c32c6cc44946ecc22ba76396fa6e859ea88bc3bc（本地）。

1. 离线对照固定现有录制、模型、配置和行为基线，不另采动作录制；后续机上 Shadow 按授权读取实时反馈并独立留证。
2. 协议、arm 流入口和反馈；原动作、servo、/1 回归。
3. Driver 迁移解算和收臂，ActuCore 只发位姿；同录制前后对照。
4. Canvas 正向/自动反馈、PICO onboarding、稳定显示与容器 APK。
5. 离线集成、真实 ROS 隔离、普通 bot 构建；空闲且具备部署授权后再现场验收。

测试覆盖：有限数值/单位/模式、过期乱序/旧会话/旧映射、租约竞争、慢 IK 不续期、同会话恢复、Shadow 零硬件输出、握把重握、收臂取消及失败、反馈边不成启动环、按端口 descriptor 不串线、旧 G1/VLA 接口、邀请过期/消费/撤销、原生深链接和升级。
独立执行回放后再完整 PICO 回放，关联输入/IK、Driver 决策、本体 cmd_pos、实测 arm/status 四段。
检查 ROS 节点/topic/type/QoS/频率；报告误差、延迟、最长中断和恢复耗时，不新增误差门槛。
分别记录代码测试、APK、镜像、部署、物理验收；不得把 mock 或构建当作真机通过。

## 交付

沿用主仓 PR #259 与 Driver PR #321，不新建重复 PR。同仓相关代码、文档、测试一起交付。
同步架构说明、卡片手册与 Driver 接口文档；当前 root STATUS 为旧历史，不用旧记录冒充本轮验收。
最终验收：用户通过 Canvas 配置配对，通过 PICO 完成天轶跟随、恢复和收臂，不依赖现场试验脚本。

## 当前实施记录

- 已实现三卡协议与端口绑定、Driver 解算/arm 流入口、自动反馈线、安装邀请、签名 APK 和配置表单。最终审查已补齐 Preview 身份释放和短暂丢反馈后保持握把的恢复握手；保留原映射，旧会话目标拒绝执行，真实故障不自动续动。以下保留逐轮证据，部署最新状态见本节末尾。
- 新 APK 0.3.18-onboarding1 已完成真实 release 构建、签名/许可检查；已发布固定构建制品，匿名读回、解压和 APK 哈希一致。未安装到实体 PICO。
- Core 配置审计补齐 Driver 接受并读回后才保存，失败不持久化候选；未知字段提前拒绝、实例入口不能绕过确认，页面显示拒绝原因。加入配置确认及统一管理鉴权后，完整 Core 回归 1451 passed / 1 skipped / 8 subtests passed。真实本地 Chrome 卡片操作及新增 Node 保存失败交互通过。ActuCore 全套 670 passed / 46 skipped；跳过项分别为 G1 数值 ABI、私有录制和需显式运行的 ROS 集成入口，不能视为通过。
- Canvas 原管理鉴权保持，补齐 LLM 与 direct/hook 调用的同一精确本机 endpoint 限制、禁重定向及凭据校验；开始不自动重放配置。真实 localhost HTTP 守卫及失败路径测试通过。
- JP6.1 与 CPU 验证镜像携带 APK；JP5.1.1 不下载、不准备遥操 APK。两份 NDK 大 NOTICE 改由固定 SDK 校验后生成；实际 Gradle 资源合并通过，生成内容与已发布 APK 字节一致，无需重发 APK。
- JVM 中生产 Java 邀请解析和 TLS PairChannel 与 localhost Python Enrollment 实际互通，15 项检查通过；单次兑换及证书不匹配时不发送邀请均有验证。不运行 Android Activity，使用测试 JSON 库及 Base64 适配，不代替 PICO 安装/浏览器唤起实测。
- 真实 ROS 集成已在本机 ARM64 禁网、只读容器单独运行：2 passed。使用真实 domain 42、Driver bus 子进程、EEF/joint/feedback topic、IK 与执行门；MCP HTTP 和本体反馈仍用有限速度模拟。Shadow 无 joint/厂商输出，Live 模拟执行及暂停确认通过；不是机器人验收。
- 现有 10 秒录制 719 帧已做迁移数值对照：718 帧有效输入；旧 ActuCore 与新 Driver 均得到 523 个有效解、195 个不可达拒绝，分类分歧为 0；输出关节最大差 6.49e-13 rad。P95 求解耗时旧 10.68 ms、新 10.36 ms。使用双方同一已有本地标定、相同逐帧实测关节和独立初值，保留 100 ms 求解预算；未找到原录制精确匹配标定，因此仅证明该标定下逐帧迁移一致，不代表原现场动作、连续调度或物理验收。私有录制及逐帧结果不提交。
- 主仓 5d0c104 的普通 Core 与 JetPack 6.1 ActuCore bot 镜像构建通过；镜像内 Core 1410、ActuCore 581 项通过。当前补丁会再次请求新 HEAD 复审，不沿用旧 HEAD 结果。Driver bot 的 rcutils 查库失败已定位到跨架构环境下 CMake 目录遍历错误码污染这一可复现机制；构建期修正的故障注入、完整本机 ARM64 构建和实际 bundle 隔离 smoke 通过，真实 EIO/缺库仍失败。Driver fc06778 已通过 bot/QEMU 完整构建（8m36s）；未修改 bot 或放宽 100 ms IK 预算。
- 主仓 66d20a8 的普通 Core/JP6.1 ActuCore 镜像构建通过，Core 镜像内 1451 项通过；ActuCore 镜像内 585 项通过、1 项失败，原因为抽取 Core wrapper 的测试未加载拆出的 stdlib helper。已在无 Core 源码 PYTHONPATH 的环境复现，并只修复测试加载：专项 96 项、全套 670 项通过，生产鉴权和拒绝断言不变，待新 HEAD bot 复验。
- 用户确认天轶空闲并开机后，已核验只读服务/目录/资源；厂商双臂反馈连续、错误码为零。Driver `388fe78` 对应固定内容快照在机上原生构建及 Python 3.10 禁网阶段 **128 项回归通过**；两轮 bundle 启停无硬件发布者。该 HEAD 的 bot 构建也已通过。未切换服务、开放控制权或发送动作，完整 ROS 和设备时间证据独立记录，不以静态反馈代替验收。
- 主仓 `3e993b5` 的 bot 镜像测试已通过：Core 1451、ActuCore 586 项。后续安装票据修复通过完整 Core **1457 passed / 1 skipped / 8 subtests passed**；实际生产 Plugin/MCP/Core/TLS 安装下载契约测试证明“嵌套 package 不匹配”为误读，正确原契约未改。下载票据已支持重启、共享 DB、多 worker、撤销及有限重试。
- 移除不再生效的遥操构建开关，普通 JP6.1 自动携带能力不变；MCP 增加 16 MiB 与 10 秒完整读取边界。真实 loopback HTTP 和隔离 Shell 专项 **39 项通过**、Core 管理 HTTP **21 项通过**，完整 ActuCore **685 passed / 46 skipped**；最新 Driver `388fe78` 的真实 DDS 隔离集成另跑 **2 passed**。压缩层体积实测及保留 G1 兼容依赖的成本已写入 ActuCore README。
- 复查时现场 Canvas 项目已再次运行，当前 Core 缺少三段编排/安装模块。继续准备保留现场改动的独立增量候选；未修改运行中的 Agent Core，也不把只换 ActuCore/Driver 称为完整卡片验收。
- 后续用户确认空闲并授权 Core 17 文件增量：以现场原镜像构建并核对原文件哈希、现有依赖版本；只新增 qrcode 8.2，禁网运行检查通过。三个目标服务已切换，既有 VLA、配对资料及 Perception 保留；项目停止且 Driver Live 门关闭。现场真实关节反馈、domain 42 EEF/IK/显示反馈通过：45 个输入、44 个关联决策、190 个反馈；ActuCore 收到本轮 IK 显示数据，joint 和厂商 cmd_pos/cmd_ctrl 均零消息，Preview 已释放。这不是 PICO 渲染或机械臂动作验收。
- 现场发现 Driver 默认注册 localhost 被三段 IP 校验排除，已修正精确本机别名规范化；注册发现、模板、绑定和生命周期相关回归 110 项通过。首次配置请求曾超时，后续同参数两次确认分别约 0.28/0.35 秒；独立导入/模型构造未复现超时，不将冷启动假设作为已确认根因。
- `d360a454` 的 localhost 兼容修复已完成现场单 Core 切换；普通 ActuCore、Driver 和 Perception 三个容器 ID 在本次切换前后不变。Canvas 读回核对通过：原 16 张接待卡完整保留，增量加入 `teleop → motion_control → arm`，共 19 张卡；新增两条正向控制线和一条自动反馈线。项目仍停止、无编辑占用，ActuCore 为 Shadow、Driver Live 门关闭；没有启动原接待卡或新增遥操执行会话。这完成部署与图配置复核，不代表 PICO 安装、实体显示、智能控制启停或物理跟随/收臂验收。
- 现场实际 Core → ActuCore 安装下载 API 已通过：安装二维码存在，下载端无需管理凭据即可用有效票据取得固定 release APK，大小 2,428,106 B，SHA256 与仓库 `package-manifest.json` 一致（`65a65c94979383d432a310eb28561358e483ca3284a16afa14e72dc9ba3e50a3`）；撤销票据后旧链接被拒绝。没有创建配对邀请或安装到 PICO。最新机上只读 ROS 复查通过，双臂反馈与错误状态正常，观察窗口内动作消息为零；仍不代表新架构物理运动验收。
- 操作手册和架构文档已同步本轮职责、配置、安装、兼容及验收边界。原物理动作记录只作旧链路基线，不用于宣布新版通过。

- 离线同步上游 `9802eae43f15`：保留导航卡、通用 `control_stream` 与网络诊断，同时保留遥操站点门、v2 会话及末端显示。合并验证：ActuCore HTTP/站点/三卡并存/导航/VLA 407 项、Core 生命周期/ROS 格式/网络/ACP 82 项、控制条 Node 9 项及本地 Chrome 遥操面板通过；本轮不部署。
- 合并上游 ROS 测试替身时，显式 DDS 测试改为先导入真实 ROS，防止替身遮蔽。首次隔离运行曾因初帧 FK 快照过期失败；合并前等价入口对照 2 项通过。修正测试握手为有界等待新鲜匹配快照，生产 100 ms 时效不变；最终禁网 DDS Shadow/Live 模拟各 1 项通过，初帧快照年龄分别约 12.7/35.4 ms，Shadow 无关节或厂商输出。该结果仍不代替硬件验收。
- `402bd1f` 的 bot Core/JP6.1 ActuCore 构建通过，镜像内 Core **1502 passed**、ActuCore **759 passed**。Driver `86de168` 增加完整 v2 启动描述符校验，本地 **240 passed**、ARM64 Python 3.10 新增专项 **111 passed**，实际 Core 转发描述符兼容；该 HEAD 的 bot 构建 `release.260922.c0db03d` 通过。两项新 HEAD 尚未切换现场，不能沿用这些回执声称已部署。
- 北京 PICO 的旧 0.3.17 与 release APK 签名不同，用户明确批准卸载迁移后保留旧 APK、安装固定哈希的 0.3.18（versionCode 22）。机器人旧配对在授权迁移中撤销；实际 Canvas 邀请 API、Android 深链接预填及连接页按钮完成配对，强制关闭应用后从普通入口启动也自动恢复同一身份连接。此项通过受信任 ADB 操作完成，不冒充 PICO 浏览器下载/安装或系统扫码验收。连接前后均为 Shadow、项目停止、Driver 无执行权，无机器人动作。
- 实体 PICO 截图确认透视、三枚操作按钮、绿色实测线、躯干与边界显示；空闲时开始按钮禁用并提示开启智能控制。随后通过现有 Canvas API 临时使用三卡图，真实 start-project/stop-project 全部通过，5 个样本确认 Shadow armed 且 Driver 无执行权；结束后完整 19 卡原图恢复、所有 tool_config 值哈希不变。独立 ROS 观察 27,095 条双臂状态、错误为零，cmd_pos/cmd_ctrl 均零消息。没有启动接待卡、模拟握把或真实动作；不以静态显示及编排代替手柄动态和物理验收。
- 对 `402bd1f` bot 意见逐项复核后，确认配置写入等底层异常会经 dispatch/info 泄露站点路径，已统一公开错误码；保留现有协议、碰撞保持及恢复原因，未知异常不原样返回。关闭异常后仍尝试 Driver 独立释放，未确认优先保留 stop_unconfirmed，清理失败保留资源和原配置供重试。完整 ActuCore **869 passed / 46 skipped**，真实 Plugin/MCP/Core/TLS 安装契约另行 **1 passed**；此补丁尚未构建或部署。安装元数据、父应用管理鉴权和 DDS 副本已有实际校验，未按误读改动正确链路；镜像增量仍按既定普通 JP6.1 能力范围记录。

- 停止回执进一步核验发现 Runtime 曾吞掉 Adapter NACK；现返回真实回执，Plugin 拒绝清理失败时保存配置。仅旧 owner 线程退出后允许串行重试 safe_stop/close，保持输入封闭、不建立新运动会话。新增 5 项真实 Runtime 零输出替身测试，相关回归最终 **208 passed**；此前 869 项结果属于前一版，不冒充本次全套。用户要求下一轮直接 Live，不再安排手柄 Shadow 输入；机上只切普通 ActuCore/Driver、保留当前 Agent Core，准备完成后由用户从 Canvas/PICO 启动动作。
